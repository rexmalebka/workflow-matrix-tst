name: workflow-tst

on:
  push

jobs:
  testing_job1:
    runs_on: ubuntu-latest
    strategy:
      matrix:
        text: ["hello", "hi", "sayonara"]
    steps:
      say_hello:
        runs: |
          echo ${{ matrix.text }}

